# Backend API (same port as server.js). Required if admin runs on another machine/port.
VITE_API_URL=http://localhost:4000

# Customer storefront URL (where /forgot-password and /reset-password live). Used if a reset link is opened on this admin app by mistake.
VITE_STORE_URL=http://localhost:5173
